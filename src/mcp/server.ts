// NOAT MCP server — implemented in Stage 4 of the build plan.
// This stub exists so the build pipeline produces all final artifacts from day one.
process.stderr.write('NOAT MCP server: not implemented yet (Stage 4)\n');
process.exit(1);
